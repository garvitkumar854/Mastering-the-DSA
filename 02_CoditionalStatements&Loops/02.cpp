// Ternary Statement
#include <iostream>
using namespace std;

int main(){
    // Syntax:
    // condition ? statement if true : statement if false;
    int n = -45;
    cout << (n>=0 ? "Positive" : "Negative");

}